{
  "name": "mini-react-repo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter ./packages/mini-react build && (pnpm --filter ./packages/mini-react run build:watch &) && pnpm --filter ./apps/playground dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.16.0",
    "@vitejs/plugin-react": "^4.4.1",
    "eslint": "^9.23.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
